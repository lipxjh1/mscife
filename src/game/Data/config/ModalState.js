/**
 * Modal State Constants
 * Extracted from CenterData.js
 *
 * Defines the possible states for modals in the application
 */

export const ModalState = {
    Open: {
        KEY: "open",
    },
    Close: {
        KEY: "closed",
    },
};

export default ModalState;
